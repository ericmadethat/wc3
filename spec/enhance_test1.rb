require_relative 'spec_helper'

describe Barracks do

before :each do
  @barracks = Barracks.new
  @unit = Unit.new(100, 25)
end

it "has and knows its HP (health points"

describe "#damage" do
  it "should reduce the Barracks' health_points by 
  half of the attack_power specified, rounding up." do
      @barracks.damage(25)
      expect(@barracks.health_points).to eq(488)
    end
  end



 

end